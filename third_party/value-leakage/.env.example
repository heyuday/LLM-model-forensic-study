# Judges (estimate + trajectory extraction). Required to judge new runs;
# not needed to analyze the shipped data.
ANTHROPIC_API_KEY=

# Target-model backends. Only the backend you sample from is required.
FIREWORKS_API_KEY=
OPENROUTER_API_KEY=
